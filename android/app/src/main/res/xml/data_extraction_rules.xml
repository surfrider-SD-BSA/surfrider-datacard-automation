<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing this app keeps goes anywhere: not to a cloud backup and not to a new
  phone. The draft is somebody's typing, the beach and their name; it lives on
  this device and is offered back from here, and nowhere else.

  allowBackup="false" in the manifest stops cloud backup, but from Android 12 it
  does not stop device-to-device transfer. These rules stop both.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
